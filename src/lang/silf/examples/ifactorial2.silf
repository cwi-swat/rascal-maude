function factorial(n)
begin
  var r;
  r := 1;
  while n > 0 do
    r := r * n;
    n := n - 1;
  od
  return r;
end

function main(void)
begin
  var x; var y;
  y := read; 
  for x := 1 to y do
    write factorial(x);
  od
end