function factorial(n)
begin
  var r;
  r := 1;
  while n > 0 do
    r := r * n;
    n := n - 1;
  od
  return r;
 end

function main(void)
begin
  var x; 
  x := read;
  while (x > 0) do
    write factorial(x);
    x := read;
  od
end